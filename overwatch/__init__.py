"""cmd-overwatch: ops dashboard and self-healing agent for Claude Code automation fleets."""

# Mirrors `project.version` in pyproject.toml, which is a separate literal —
# there is no build-time sync between the two, so bumping one without the
# other is a real drift risk on release.
__version__ = "0.1.0"
